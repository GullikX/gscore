image: alpine/latest

packages:
  - fluidsynth-dev
  - gcc
  - glew-dev
  - glfw-dev
  - libx11-dev
  - libxml2-dev
  - make

sources:
  - https://git.sr.ht/~gullik/gscore

tasks:
  - build: |
      cd gscore
      make
